---

variables:
  CI_UPLOAD_DOCUMENTATION: "yes"
  CI_EL9: "yes"

include:
  - project: 'dpres/gitlab-ci-pipeline'
    ref: master
    file:
      - '/pipeline/common.yml'
      - '/pipeline/fddps.yml'
      - '/pipeline/sapa.yml'
